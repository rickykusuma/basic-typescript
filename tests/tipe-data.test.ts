
describe("Data Type",()=>{
    it('should must declare', () => {
        let name: string = 'Ricky';
        const balance: number = 10000;
        const isVip: boolean =true

        console.info(name)
        console.info(balance)
        console.info(isVip)
    });
})